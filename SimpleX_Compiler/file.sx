//This is a demobstration of my Tokenizer functionality

class File 
{ 
    field Array arr1[100];
    method void main()
    {
        int x, y, z;
        let x = 3;
        let y = 25;

        //let z = (x * y);
        let z = Math.sqrt(49);

        while(1)
        {}
    }
}