//This is a demobstration of my Tokenizer functionality

class Math { 
    field Array arr1[100];
    field Array arr2[100];
    method void main()
    {
        int x, y;
        let x = 1;
        let y = 0;
        while(y < 99)
        {
           let arr1[y]     = x; 
           let x = x + 1;
           let y = y + 1;
        }      
    }
}