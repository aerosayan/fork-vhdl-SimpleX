//This is a demobstration of my Tokenizer functionality

class Point { 
    static int xPosition, yPosition;
    field boolean myVar ;

    constructor int Point(int x, int y)
    {
       let xPosition = x;
       let yPosition = y;
    }

    method int getx(int y) 
    {
        let xPosition = (x - 1) + (y / 2);
        let y = (7);
    }
    
}