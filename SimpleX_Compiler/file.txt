//This is a demobstration of my Tokenizer functionality

class Square { 
    field int x, y;
    static int pointCount;
    field int z,l;
    static boolean isFantastic;
    

    constructor void Square(int pointx, int pointy)
    {
        let x = pointx;
        let y = pointy;
    }

    method void incSize() 
    {
        int x, y;
        if( x > y)
        {
           let size = size + 2;
           while((x > y) & (z > y))
           {
               let x = x + 1 - (y);
           }
        }
    }

}