//This is a demobstration of my Tokenizer functionality

class Square { 
    field int x, y;
    static int pointCount;
    field boolean go;
      

    constructor void Square(int pointx, int pointy)
    {
        int r , u;
        let x = pointx[3] * ty(1, (x+y));
        let y = pointy;
    }

    method int distance(Point other) 
    {
        int dx, dy, size;
        let dx = x - 1;
        let dy = y - 3;
        return;
    }

    
}